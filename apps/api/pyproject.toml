[project]
name = "medpath-real-api"
version = "0.1.0"
description = "MedPath Research & Education Skills Studio FastAPI backend"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
