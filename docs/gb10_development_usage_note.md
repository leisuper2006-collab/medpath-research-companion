# GB10 开发使用说明

GB10 只能作为开发执行环境，用于写代码、安装依赖、运行测试、构建前端、生成 demo 数据和批量检查。产品文案中统一称为 Private Runtime Server / 私有化 AI Skills 运行与模拟案例生成服务器环境，不把 GB10 写成正式产品服务器。

本项目不保存 SSH 密码、私钥、token、cookie 或真实患者数据。如果需要在 GB10 上执行构建，应由用户在远程环境中自行配置凭据，结果通过 Git、zip 或 rsync 拉回本地。

本轮本地环境中尝试解析 `gb10` 主机名失败，因此未使用 GB10 执行开发或测试。

